// Suite-level product name shown in the landing UI. Default is the open-source suite name.
// A downstream distribution (e.g. the ViscoForge image) overrides this by replacing this file
// with one that sets a different value — see index.html, which reads window.__viscoProductName.
window.__viscoProductName = 'ViscoSuite';
